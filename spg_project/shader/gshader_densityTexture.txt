#version 410 core
uniform int lr;

layout (triangles) in;
layout (triangle_strip, max_vertices = 3) out;
out int gl_Layer;

void main(void){
//pass-thru!
for(int i=0; i<3; i++)
  {
    gl_Position = gl_in[i].gl_Position;
	gl_Position = gl_in[i].gl_Position;
    EmitVertex();
  }
gl_Layer = lr;
EndPrimitive();
}